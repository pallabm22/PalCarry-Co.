<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head') %>

  <body class="font-['helvetica_now_display']">
    <% if(showHeader) { %> <%- include('./partials/navbar') %> <% } %> <%
    if(error.length > 0) { %>
    <div
      class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-red-500"
    >
      <span class="inline-block mt-1 mb-1 text-white"> <%= error %> </span>
    </div>
    <% } else if(message.length > 0) { %>
    <div
      class="absolute top-10 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500"
    >
      <span class="inline-block mt-1 mb-1 text-white"> <%= message %> </span>
    </div>
    <% } %>

    <div class="w-full h-screen flex px-20">
      <div class="w-1/2 flex items-center justify-center h-screen">
        <div class="w-full px-32">
          <h3 class="text-4xl">
            welcome to
            <span class="text-blue-400 font-semibold">PalCarry Co.</span>
          </h3>
          <h4 class="text-2xl mb-5">create your account</h4>
          <form autocomplete="off" action="/user/register" method="post">
            <input
              class="bg-zinc-100 block w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
              type="text"
              placeholder="Full Name"
              name="fullname"
            />
            <input
              class="bg-zinc-100 block w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
              type="email"
              placeholder="Email"
              name="email"
            />
            <input
              class="bg-zinc-100 block w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
              type="password"
              placeholder="Password"
              name="password"
            />
            <input
              class="px-5 rounded-full py-3 mt-2 bg-blue-500 text-white"
              type="submit"
              value="Create My Account"
            />
          </form>
        </div>
      </div>

      <div
        class="w-1/2 flex flex-col items-center justify-center h-screen px-32"
      >
        <div class="w-full">
          <h4 class="text-2xl capitalize mb-5">login your account</h4>
          <form autocomplete="off" action="/user/login" method="post">
            <input
              class="block bg-zinc-100 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
              type="email"
              placeholder="Email"
              name="email"
            />
            <input
              class="block bg-zinc-100 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
              type="password"
              placeholder="Password"
              name="password"
            />
            <input
              class="px-5 block rounded-full py-3 mt-2 bg-blue-500 text-white"
              type="submit"
              value="Login"
            />
          </form>
        </div>

        <!-- ✅ Admin button below login form -->
        <div class="mt-8 w-full">
          <h4 class="text-2xl capitalize mb-5 text-center">admin login</h4>
          <a
            href="/owner/login"
            class="block text-center px-5 rounded-full py-3 bg-red-500 hover:bg-red-600 text-white transition"
          >
            Go to Admin Login
          </a>
        </div>
      </div>
    </div>

    <%- include('./partials/footer') %>
  </body>
</html>
